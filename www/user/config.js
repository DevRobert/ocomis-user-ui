// This config is only used for local development
// The config for all other stages is generated on the fly and served by the hapi server

window.ocomisUserUiConfig = {
    "apis": {
        "user": {
            "uri": "http://localhost/user/api"
        }
    }
}
